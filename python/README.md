# [WIP] Python bindings for Spark Ensemble

## Install
In `python` folder:
```bash
pip3 install .
```

If you want only install requirements:
```bash
pip3 install -r requirements.txt
```

## Test
In `python` folder:
```bash
python3 -m unittest
```